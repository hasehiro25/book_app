<h1>フォロー</h1>
<h2>フォロー(<%= @followings.count %>)</h2>
<ul>
  <% @followings.each do |following| %>
    <li><%= link_to following.followed.name, user_path(following.followed) %></li>
  <% end %>
</ul>
