<%= form_with model: comment, local: true do |f| %>
  <div class= form-group>
    <%= f.label :content%>
    <%= f.text_area :content, class: "form-control"%>
  </div>
  <div class="form-group">
    <%= f.submit %>
  </div>
<% end %>
